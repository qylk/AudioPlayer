<?xml version="1.0" encoding="utf-8"?>
<!-- 歌曲列表 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:descendantFocusability="blocksDescendants"
    android:orientation="horizontal" >

    <ImageView
        android:id="@+id/music"
        android:layout_width="45dip"
        android:layout_height="45dip"
        android:padding="5dip"
        android:src="@drawable/songimage" /> <!-- 固定图标 -->

    <LinearLayout
        android:layout_width="240dp"
        android:layout_height="45dip"
        android:gravity="center_vertical"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/ltitle"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:ellipsize="end"
            android:singleLine="true"
            android:text="title"
            android:textAppearance="@android:style/TextAppearance.Large" /> <!-- 歌曲标题 -->

        <TextView
            android:id="@+id/lartist"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="artist"
            android:textColor="@android:color/white"
            android:textSize="12sp" /> <!-- 歌手名 -->
    </LinearLayout>

    <ImageButton
        android:id="@+id/checkBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:background="@drawable/checkbox_unchecked"
        android:visibility="invisible"
        android:padding="5dp" /> <!-- 选择框 -->

</LinearLayout>